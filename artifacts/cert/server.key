# REMOVED: Private key was committed to version control and must be rotated.
# This key has been exposed in git history and should be considered compromised.
#
# To provide a new server key:
#   1. Generate a new RSA private key (e.g., openssl genrsa -out server.key 2048)
#   2. Place it at this path, or set the TLS_KEY_PATH environment variable
#   3. Ensure this file is listed in .gitignore to prevent future exposure
#   4. Rotate/revoke the old key as it remains in git history
