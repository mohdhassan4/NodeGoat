# Private key removed from source control.
# Provide the TLS private key via the TLS_KEY_PATH environment variable
# pointing to the key file on disk, or via TLS_KEY containing the
# PEM-encoded key content directly.
